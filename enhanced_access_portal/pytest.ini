[pytest]
DJANGO_SETTINGS_MODULE = enhanced_access_portal.settings
; Django's convention is a bare "tests.py" per app, which pytest's default
; python_files (test_*.py, *_test.py) would never discover on its own.
python_files = tests.py test_*.py *_tests.py
